{
  "hooks": [
    {
      "event": "SessionStart",
      "description": "Load project context and high-confidence patterns into session",
      "command": "${CLAUDE_PLUGIN_ROOT}/hooks/scripts/session-start.sh"
    },
    {
      "event": "PreCompact",
      "description": "Persist pipeline state and working decisions before context compaction",
      "command": "${CLAUDE_PLUGIN_ROOT}/hooks/scripts/pre-compact.sh"
    },
    {
      "event": "PostCompact",
      "description": "Restore pipeline state and inject relevant patterns after compaction",
      "command": "${CLAUDE_PLUGIN_ROOT}/hooks/scripts/post-compact.sh"
    }
  ]
}
